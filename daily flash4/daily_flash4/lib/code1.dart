import 'package:flutter/material.dart';

class code1 extends StatelessWidget {
  const code1({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("center elevated button"),
      ),
    );
  }
}
